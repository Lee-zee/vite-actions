on: push

jobs: 
  npm-build:
    name: npm-build执行
    runs-on: ubuntu-latest
    steps:
      # 使用checkout将文件复制到ubuntu系统中
      - name: 读取仓库内容
        uses: actions/checkout@v4

      - name: 安装依赖 ⬇️
        run: npm install
      
      - name: 打包项目 📦
        run: npm run build